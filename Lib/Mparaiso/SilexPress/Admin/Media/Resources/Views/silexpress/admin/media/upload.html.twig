<ul>
    {% for file in files %}
        <h5>{{ file.file.name }}</h5>
        <img height="100px" src="/admin/media/upload/{{ file.file._id }}" alt="{{ file.file.name }}"/>
        <pre>
{{ file|json_encode(constant('JSON_PRETTY_PRINT')) }}
    </pre>
    {% endfor %}
</ul>
